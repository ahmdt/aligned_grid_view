# aligned_grid_view

## Package Boundary

- This is a Flutter library package. Keep its public API rooted at `lib/aligned_grid_view.dart`; tests import it as `package:aligned_grid_view/aligned_grid_view.dart`.
- Do not commit `pubspec.lock`; library lockfiles are intentionally ignored.

## Development

- Fetch dependencies with `flutter pub get`.
- Run static analysis with `flutter analyze` and the full test suite with `flutter test`.
- Run one test file with `flutter test test/aligned_grid_view_test.dart`.
- Lint rules come solely from `package:flutter_lints/flutter.yaml`; there are no repository-specific analysis overrides.
