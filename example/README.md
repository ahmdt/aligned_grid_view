# aligned_grid_view example

An interactive showcase for `AlignedGridView.count`, `AlignedGridView.extent`,
and `SliverAlignedGrid`.

Run it from this directory:

```sh
flutter pub get
flutter run
```
